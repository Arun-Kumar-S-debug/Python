class Solution:
    def reverseBits(self,n):
        a=str(n)
        l=[]
        for i in a:
            l.append(int(i))
        n=0
        for i in range(0,len(l)):
            n=n+(l[i]*2**i)
        print(n)

n=input()
h1=Solution()
h1.reverseBits(n)
